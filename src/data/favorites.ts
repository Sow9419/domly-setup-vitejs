import { Property } from './properties';

export const addToFavorites = (property: Property) => {
  // Implementation would go here - for now just console log
  console.log('Adding to favorites:', property);
};

export const removeFromFavorites = (propertyId: string) => {
  // Implementation would go here - for now just console log
  console.log('Removing from favorites:', propertyId);
};